\documentclass[border=20pt]{standalone}
\input{template.def}

% COMPUTER(MAC(PK))
% COMPUTER_COLOR(Compter (PK, FK referencing COMPUTER.MAC), Name, Email)

\Frame(0,0){1}[COMPUTER]{
	MAC/PK};

\Frame(5, 0){2}[COMPUTER\_USER]{
	Computer/PK,
	Name/PK,
	Email/PK};

\draw[FK] % From COMPUTER_COLOR.Desk to COMPUTER.MAC
(MAC1)++(0,0) -- ++(0,-.5) -- ++(-0,0) coordinate (inter)
-- (Computer2 |- inter) --++(0,0.5);
\end{tikzpicture}
\end{document}