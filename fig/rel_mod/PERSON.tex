\documentclass[border=20pt]{standalone}
\input{template.def}

% PERSON(SSN (PK), DOB, ADDRESS (FK to PLACE.Address)
% ADDRESS(Address (PK), Rooms)

\Frame(0,0){1}[NAME]{
	SSN/PK, 
	DOB/A,
	StaysAt/A};

\Frame(5, 0){2}[ADDRESS]{
	Address/PK,
	Rooms/A};

\draw[FK] % From Habitants2 to Id1
(Address2)++(0,0) -- ++(0,-.5) -- ++(-0,0) coordinate (inter)
-- (StaysAt1 |- inter) --++(0,0.6);
\end{tikzpicture}
\end{document}